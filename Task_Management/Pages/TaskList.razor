@page "/TaskList"
@inject TasksService tasksService
@using Task_Management.Model
@using Task_Management.Service
@using System.Collections.Generic


@* @if (tasks == null)
{
    <p>Loading tasks...</p>
}
else
{ *@
   <h3>Lista de Tareas y Usuarios</h3>
<a class="btn btn-primary" href="/Task">Agregar nuevo</a>
<a class="btn btn-primary" href="/">Inicio</a>

<table class="table">
    <thead>
        <tr>
            <th>ID Tarea</th>
            <th>Título</th>
            <th>Descripción</th>
            <th>Fecha de Vencimiento</th>
            <th>Completada</th>
            <th>Prioridad</th>
            <th>ID Usuario</th>
            <th>Nombre Usuario</th>
            <th>Correo Electrónico</th>
            <th>Activo</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var task in tasks)
        {
            <tr>
                <td>@task.Id</td>
                <td>@task.Title</td>
                <td>@task.Description</td>
                <td>@task.DueDate.ToShortDateString()</td>
                <td>
                    <input type="checkbox" @bind="task.IsCompleted" disabled="@(task.IsCompleted ? "disabled" : null)" />
                </td>
                <td>@task.Priority</td>
                <td>@task.UserId</td>
            </tr>
        }
    </tbody>
</table>
@* } *@

@code {
    private List<TaskModel> tasks;
    

    public void OnInitialized()
    {
        if (tasksService != null)
        {
            tasks =  tasksService.GetTasks(1).ToList(); // Reemplaza 1 con el ID de usuario real
        }
        else
        {
            <p>No tasks available.</p>
        }
    }
}

@* <h3>Lista de Tareas y Usuarios</h3>
<a class="btn btn-primary" href="/Task">Agregar nuevo</a>

<table class="table">
    <thead>
        <tr>
            <th>ID Tarea</th>
            <th>Título</th>
            <th>Descripción</th>
            <th>Fecha de Vencimiento</th>
            <th>Completada</th>
            <th>Prioridad</th>
            <th>ID Usuario</th>
            <th>Nombre Usuario</th>
            <th>Correo Electrónico</th>
            <th>Activo</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var taskModel in tasks)
        {
            <tr>
                <td>@taskModel.Id</td>
                <td>@taskModel.Title</td>
                <td>@taskModel.Description</td>
                <td>@taskModel.DueDate.ToShortDateString()</td>
                <td>
                    <input  type="checkbox" @bind="taskModel.IsCompleted" disabled="@(taskModel.IsCompleted ? "disabled" : null)" />
                </td>
                <td>@taskModel.Priority</td>
                <td>@taskModel.UserId</td>
                <td>@taskModel.User.Name</td>
                <td>@taskModel.User.Email</td>
                <td>@taskModel.User.IsActive</td>
            </tr>
        }
    </tbody>
</table>

@code {
    List<Task_Management.Model.TaskModel> tasks;

    protected override void OnInitialized()
    {
        tasks = tasksService.GetTasks().ToList();
    }
}
 *@